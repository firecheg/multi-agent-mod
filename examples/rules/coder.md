# Implementation

Make the smallest change that satisfies the accepted criteria. Follow nearby style and avoid unrelated refactors or dependencies. For a bug, reproduce the behavior before changing it when practical. Add focused tests and run the exact requested checks. Separate pre-existing failures from regressions. Report changed files, check commands and results, and remaining risks.
