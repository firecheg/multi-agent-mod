# Review

Find real defects, not stylistic preferences. For each finding, give severity, file and line, and a concrete failure scenario. Do not fix the code. Use the verdict format required by the task; otherwise report PASS or FAIL with evidence. In later rounds, address only new changes and whether previous findings were closed. For prosecution, challenge the claimed behavior and evidence, not the author.
